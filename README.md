# plandoc-protocol

`plandoc-protocol` is a skill that turns an LLM into a state machine to get more reliable results.

It provides a concrete Plandoc + Design Doc workflow for planning, execution, review, and protocol sync.
