interface:
  display_name: "Plandoc Protocol"
  short_description: "Strict state-machine plandoc workflow"
  default_prompt: "Use $plandoc-protocol as a strict state-machine workflow: seed, refine, review, activate, implement, verify, and resolve with explicit stage anchoring."

policy:
  allow_implicit_invocation: true
